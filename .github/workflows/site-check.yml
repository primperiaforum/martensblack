name: Site check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  local-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check local site paths
        shell: pwsh
        run: ./tools/check-site.ps1
